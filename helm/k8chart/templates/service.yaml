apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-k8chart
  labels:
    app: {{ .Release.Name }}-k8chart
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ .Release.Name }}-k8chart